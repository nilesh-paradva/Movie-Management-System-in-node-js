<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Single Movie</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <div class="card" style="width: 100%; max-width: 600px; margin: 0 auto;">
            <img src="<%= MovieData.path %>" class="card-img-top" alt="<%= MovieData.title %>" width="200px">
            <div class="card-body">
                <h5 class="card-title"><%= MovieData.title %></h5>
                <p class="card-text"><strong>Description:</strong> <%= MovieData.description %></p>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item"><strong>Release Year:</strong> <%= MovieData.releaseYear %></li>
                    <li class="list-group-item"><strong>Running Time:</strong> <%= MovieData.runningTime %> minutes</li>
                    <li class="list-group-item"><strong>Quality:</strong> <%= MovieData.quality %></li>
                    <li class="list-group-item"><strong>Country:</strong> <%= MovieData.country %></li>
                    <li class="list-group-item"><strong>Genre:</strong> <%= MovieData.genre %></li>
                    <li class="list-group-item"><strong>Item Type:</strong> <%= MovieData.itemType %></li>
                    <li class="list-group-item"><strong>Video Upload:</strong> <%= MovieData.videoUpload %></li>
                    <li class="list-group-item"><strong>Link:</strong> <a href="<%= MovieData.link %>" target="_blank">Watch Now</a></li>
                    <!-- <li class="list-group-item"><strong>Path:</strong> <%= MovieData.path %></li> -->
                </ul>
            </div>
        </div>
    </div>
</body>
</html>