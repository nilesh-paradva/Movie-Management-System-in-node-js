<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Movie</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="bg-light">
    <section class="container py-5">
        <div class="card shadow-lg">
            <div class="card-body">
                <h2 class="card-title text-center mb-4">Edit Movie</h2>
                <form action="/updateMovies/<%= MovieData._id %>" method="post" enctype="multipart/form-data">
                    <div class="mb-3">
                        <label for="file-upload" class="form-label">Upload Cover</label>
                        <input type="file" class="form-control" id="file-upload" name="posterimage">
                        <div class="form-text">PNG, JPG up to 2MB</div>
                    </div>

                    <div class="mb-3">
                        <label for="title" class="form-label">Title</label>
                        <input type="text" class="form-control" name="title" id="title" value="<%= MovieData.title %>" placeholder="Enter title (minimum 2 characters)">
                    </div>

                    <div class="mb-3">
                        <label for="description" class="form-label">Description</label>
                        <textarea class="form-control" id="description" name="description" rows="3" placeholder="Enter description"><%= MovieData.description %></textarea>
                    </div>

                    <div class="mb-3">
                        <label for="releaseYear" class="form-label">Release Year (dd-mm-yyyy)</label>
                        <input type="text" class="form-control" name="releaseYear" id="releaseYear" value="<%= MovieData.releaseYear %>" placeholder="Enter release year">
                    </div>

                    <div class="mb-3">
                        <label for="runningTime" class="form-label">Running Time (min)</label>
                        <input type="text" class="form-control" name="runningTime" id="runningTime" value="<%= MovieData.runningTime %>" placeholder="Enter running time">
                    </div>

                    <div class="mb-3">
                        <label for="quality" class="form-label">Quality</label>
                        <select class="form-select" id="quality" name="quality">
                            <option value="HD" <%= MovieData.quality === 'HD' ? 'selected' : '' %>>HD</option>
                            <option value="Full HD" <%= MovieData.quality === 'Full HD' ? 'selected' : '' %>>Full HD</option>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label for="country" class="form-label">Country</label>
                        <input type="text" class="form-control" name="country" id="country" value="<%= MovieData.country %>" placeholder="Enter country">
                    </div>

                    <div class="mb-3">
                        <label for="genre" class="form-label">Genre</label>
                        <input type="text" class="form-control" name="genre" id="genre" value="<%= MovieData.genre %>" placeholder="Enter genre">
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Item Type</label>
                        <div class="form-check">
                            <input class="form-check-input" type="radio" name="itemType" id="movie" value="movie" <%= MovieData.itemType === 'movie' ? 'checked' : '' %>>
                            <label class="form-check-label" for="movie">Movie</label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input" type="radio" name="itemType" id="tvshow" value="tvshow" <%= MovieData.itemType === 'tvshow' ? 'checked' : '' %>>
                            <label class="form-check-label" for="tvshow">TV Show</label>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label for="link" class="form-label">Link</label>
                        <input type="text" class="form-control" name="link" id="link" value="<%= MovieData.link %>" placeholder="https://">
                    </div>

                    <div class="text-end">
                        <button type="submit" class="btn btn-primary">Update</button>
                    </div>
                </form>
            </div>
        </div>
    </section>
</body>
</html>